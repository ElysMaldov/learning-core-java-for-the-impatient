In the `ScopeDemo` program, modify the contents of the `text` or `count` variables. Do it both inside and outside the lambda expression. What happens?

What can you do if you really want to do a mutation?
