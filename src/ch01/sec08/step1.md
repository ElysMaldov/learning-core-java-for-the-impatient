Both arrays and array lists are indexed sequences of elements.

Run the `ArrayDemo` and `ArrayListDemo` programs and look at the source files. What are the principal differences that you see between arrays and array lists in Java?